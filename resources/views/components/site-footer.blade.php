<footer class="bg-charcoal pt-16 text-stone"><div class="shell grid gap-10 pb-12 md:grid-cols-2 lg:grid-cols-4"><div><x-logo /><p class="mt-6 text-sm leading-7">{{ config('lawoffice.office.description') }}</p></div><div><h2 class="footer-title">التنقل</h2>@foreach(config('lawoffice.nav') as $item)<a class="footer-link" href="{{ route($item['route']) }}">{{ $item['label'] }}</a>@endforeach</div><div><h2 class="footer-title">مجالات الممارسة</h2>@foreach(config('lawoffice.practices') as $slug => $practice)<a class="footer-link" href="{{ route('practice-area',$slug) }}">{{ $practice['title'] }}</a>@endforeach</div><div><h2 class="footer-title">معلومات التواصل</h2><p class="text-sm leading-7">{{ config('lawoffice.contact.address') }}<br>{{ config('lawoffice.contact.phone') }}<br>{{ config('lawoffice.contact.email') }}</p></div></div><div class="border-t border-white/10"><div class="shell flex flex-col justify-between gap-4 py-6 text-xs md:flex-row"><p>المعلومات المنشورة على هذا الموقع هي معلومات عامة ولا تُعد بديلاً عن الاستشارة القانونية المتخصصة.</p><p>© {{ now()->year }} {{ config('lawoffice.office.short_name') }} · <a href="{{route('privacy')}}">الخصوصية</a> · <a href="{{route('disclaimer')}}">إخلاء المسؤولية</a></p></div></div></footer>
